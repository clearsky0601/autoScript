shebang

# 更新软件源



# 安装zsh


# 将zsh设置为默认的shell


# 下载安装oh-my-zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"





# 安装自动补全插件\代码高亮插件

# https://github.com/zsh-users/zsh-autosuggestions/blob/master/INSTALL.md#oh-my-zsh
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions

git clone https://github.com/zsh-users/zsh-syntax-highlighting.git
echo "source ${(q-)PWD}/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh" >> ${ZDOTDIR:-$HOME}/.zshrc

# 修改zshrc
    #修改主题
    # 将ZSH_THEME="robbyrussell"改为ZSH_THEME="ys"
    # 将ZSH_THEME=""改为ZSH_THEME="af-magic"
    ZSH_THEME="af-magic"
    # 将plugins=(git)改为plugins=(git zsh-autosuggestions zsh-syntax-highlighting)
    plugins=(git zsh-autosuggestions zsh-syntax-highlighting)






source ~/zshrc




---
源需求文档




shebang

# 在家目录下创建workSpace目录
mkdir $HOME/workSpace

#!/bin/bash


# 检查是否为root用户
if [ "$EUID" -ne 0 ]; then
    SUDO='sudo'
    echo "请提供管理员密码以继续安装:"
else
    SUDO=''
fi

#更新软件源
apt update



# 必装软件
# 检查是否存在然后安装 下面的软件包

git,vim,nnn,python,wget,curl,tree


# 配置全局git:用户名为clearsky0601,邮箱为xinyuscl@163.com
git config --global user.name "clearsky0601"
git config --global user.email "xinyuscl@163.com"

# 克隆我的配置仓库到workSpace目录下
git clone https://github.com/clearsky0601/autoScript.git $HOME/workSpace/autoScript

# 配置vimrc
将autoScript/vimrc 复制到 $HOME 目录下

